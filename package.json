{
  "name": "red-viral-monitor",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:dist": "node scripts/build-dist.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vitest": "^1.6.0"
  }
}
